import { Dot } from "lucide-react";

export function LargePoint() {
	return (
		<span className="text-text-tertiary select-none text-xs inline-flex items-center">
			<Dot size={18} aria-hidden="true" />
		</span>
	);
}
